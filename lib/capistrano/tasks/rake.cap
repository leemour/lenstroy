namespace :sake do
  namespace :sitemaps do
    task :refresh do
      on roles(:app) do
        # within doesn't work, using cd manually
        within current_path do
          execute "cd #{current_path}"
          with rack_env: 'production' do
             execute :rake, "sitemaps:refresh"
          end
        end
      end
    end
  end
end